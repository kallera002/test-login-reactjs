import React, { Fragment } from "react";

import Navbar from "./../layouts/navbar";

const Router = props => {
  return (
    <Fragment>
      <div>
        <Navbar />
      </div>
    </Fragment>
  );
};

export default Router;
