import React, { Fragment } from 'react'

const Button = (props) =>{
    return (
        <Fragment>
            <button onClick={props.click}>lorem</button>
        </Fragment>
    )
}

export default Button